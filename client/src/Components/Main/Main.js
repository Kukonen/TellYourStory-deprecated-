import React from 'react';
import './Main.scss'
import {observer} from "mobx-react-lite";

const Main = observer(() => {
    return (
        <div>
            
        </div>
    )
});

export default Main;