import React from 'react'
import './SavedStories.scss'
import {observer} from "mobx-react-lite";

const SavedStories = observer(() => {
    return (
        <div>
            Saved Stories
        </div>
    )
})

export default SavedStories;